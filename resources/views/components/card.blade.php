<div class="card ms-2 me-2 mt-2 mb-2">
    <a href="{{ $href }}">
        <figure>
            <img src="{{ $img }}" alt="{{ $title }}">
        </figure>
        <div class="card-body">
            <h3>{{ $title }}</h3>
            <p class="general-text">{{ $text }}</p>
        </div>
    </a>
</div>
